scripted_gui = {	
	SUN_SCW_event = {
		context_type = player_context
		window_name = "SUN_SCW_window"

		visible = {
			has_country_flag = SCW_super
		}
	}

	SUN_SCW_frame = {
		context_type = player_context
		window_name = "SUN_SCW_custom"

		visible = {
			has_country_flag = SCW_super
		}

		effects = {
			Option_click = {
				clr_country_flag = SCW_super
			}
		}
	}

	SUN_CW_event = {
		context_type = player_context
		window_name = "SUN_CW_window"

		visible = {
			has_country_flag = CW_super
		}
	}

	SUN_CW_frame = {
		context_type = player_context
		window_name = "SUN_CW_super"

		visible = {
			has_country_flag = CW_super
		}

		effects = {
			Option_click = {
				clr_country_flag = CW_super
			}
		}
	}

	british_invasion_event = {
		context_type = player_context
		window_name = "british_invasion_window"

		visible = {
			has_country_flag = redcoat
		}
	}

	british_invasion_frame = {
		context_type = player_context
		window_name = "british_invasion_super"

		visible = {
			has_country_flag = redcoat
		}

		effects = {
			Option_click = {
				clr_country_flag = redcoat
			}
		}
	}

	sablin_chungus_event = {
		context_type = player_context
		window_name = "sablin_chungus_window"

		visible = {
			has_country_flag = sablin
		}
	}

	sablin_chungus_frame = {
		context_type = player_context
		window_name = "sablin_chungus_super"

		visible = {
			has_country_flag = sablin
		}

		effects = {
			Option_click = {
				clr_country_flag = sablin
			}
		}
	}

	hasona_conoli_event = {
		context_type = player_context
		window_name = "hasona_conoli_window"

		visible = {
			has_country_flag = hasona_conoli_super
		}
	}

	hasona_conoli_frame = {
		context_type = player_context
		window_name = "hasona_conoli_super"

		visible = {
			has_country_flag = hasona_conoli_super
		}

		effects = {
			Option_click = {
				clr_country_flag = hasona_conoli_super
			}
		}
	}

	hasona_absoli_event = {
		context_type = player_context
		window_name = "hasona_absoli_window"

		visible = {
			has_country_flag = hasona_absoli_super
		}
	}

	hasona_absoli_frame = {
		context_type = player_context
		window_name = "hasona_absoli_super"

		visible = {
			has_country_flag = hasona_absoli_super
		}

		effects = {
			Option_click = {
				clr_country_flag = hasona_absoli_super
			}
		}
	}

	hasona_despotism_event = {
		context_type = player_context
		window_name = "hasona_despotism_window"

		visible = {
			has_country_flag = hasona_despotism_super
		}
	}

	hasona_despotism_frame = {
		context_type = player_context
		window_name = "hasona_despotism_super"

		visible = {
			has_country_flag = hasona_despotism_super
		}

		effects = {
			Option_click = {
				clr_country_flag = hasona_despotism_super
			}
		}
	}

	yin_despotism_event = {
		context_type = player_context
		window_name = "yin_despotism_window"

		visible = {
			has_country_flag = yin_despotism_super
		}
	}

	yin_despotism_frame = {
		context_type = player_context
		window_name = "yin_despotism_super"

		visible = {
			has_country_flag = yin_despotism_super
		}

		effects = {
			Option_click = {
				clr_country_flag = yin_despotism_super
			}
		}
	}

	novvan_super_event = {
		context_type = player_context
		window_name = "novvan_super_window"

		visible = {
			has_country_flag = novvan_super
		}
	}

	novvan_super_frame = {
		context_type = player_context
		window_name = "novvan_super_super"

		visible = {
			has_country_flag = novvan_super
		}

		effects = {
			Option_click = {
				clr_country_flag = novvan_super
			}
		}
	}

	yin_conoli_event = {
		context_type = player_context
		window_name = "yin_conoli_window"

		visible = {
			has_country_flag = yin_conoli
		}
	}

	yin_conoli_frame = {
		context_type = player_context
		window_name = "yin_conoli_super"

		visible = {
			has_country_flag = yin_conoli
		}

		effects = {
			Option_click = {
				clr_country_flag = yin_conoli
			}
		}
	}

	skm_super_event = {
		context_type = player_context
		window_name = "skm_super_window"

		visible = {
			has_country_flag = skm_super
		}
	}

	skm_super_frame = {
		context_type = player_context
		window_name = "skm_super_super"

		visible = {
			has_country_flag = skm_super
		}

		effects = {
			Option_click = {
				clr_country_flag = skm_super
			}
		}
	}

	chaos_super_event = {
		context_type = player_context
		window_name = "chaos_super_window"

		visible = {
			has_global_flag = chaos_super
		}
	}

	chaos_super_frame = {
		context_type = player_context
		window_name = "chaos_super_super"

		visible = {
			has_global_flag = chaos_super
		}

		effects = {
			Option_click = {
				clr_global_flag = chaos_super
			}
		}
	}

	victory_super_event = {
		context_type = player_context
		window_name = "victory_super_window"

		visible = {
			has_global_flag = chaos_end
		}
	}

	victory_super_frame = {
		context_type = player_context
		window_name = "victory_super_super"

		visible = {
			has_global_flag = chaos_end
		}

		effects = {
			Option_click = {
				clr_global_flag = chaos_end
			}
		}
	}
	

	ncw_super_event = {
		context_type = player_context
		window_name = "ncw_super_window"

		visible = {
			has_global_flag = ncw_super
		}
	}

	ncw_super_frame = {
		context_type = player_context
		window_name = "ncw_super_super"

		visible = {
			has_global_flag = ncw_super
		}

		effects = {
			Option_click = {
				clr_global_flag = ncw_super
			}
		}
	}
}